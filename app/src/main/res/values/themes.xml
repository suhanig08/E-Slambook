<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.E_Slambook" parent="Theme.Material3.Light">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/transparent</item>
    </style>

    <style name="Base.Theme.AppCompat.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/transparent</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.E_Slambook" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>